name: Hamobus Data Logger

on:
  repository_dispatch:
    types: [hamobus_qr_scan]

# Aynı anda birden fazla okutma gelirse sıraya koyar, iptal etmez.
concurrency:
  group: hamobus-logging
  cancel-in-progress: false 

permissions:
  contents: write

jobs:
  update-json:
    runs-on: ubuntu-latest
    steps:
      - name: Depoyu Kontrol Et
        uses: actions/checkout@v4

      - name: Node.js Kurulumu
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: JSON Guncelle (Ana Dizin Modu)
        # Veriyi PAYLOAD ismiyle guvenli bir sekilde sisteme aliyoruz
        env:
          PAYLOAD: ${{ toJson(github.event.client_payload) }}
        run: |
          node -e "
          const fs = require('fs');
          
          // DOSYA YOLU: Artik 'QR/' yok, direkt ana dizindeki dosyaya yazar.
          const filePath = 'hamobus.json'; 
          
          try {
            const payloadRaw = process.env.PAYLOAD;
            const newEntry = JSON.parse(payloadRaw);
            newEntry.timestamp = new Date().toISOString();

            let data = [];
            // Dosya varsa oku, yoksa bos dizi baslat
            if (fs.existsSync(filePath)) {
              const fileContent = fs.readFileSync(filePath, 'utf8').trim();
              data = fileContent ? JSON.parse(fileContent) : [];
            }

            // Yeni veriyi en basa ekle
            data.unshift(newEntry);

            // 14 gunluk temizlik (Sadece taze verileri tutar)
            const threshold = new Date();
            threshold.setDate(threshold.getDate() - 14);
            data = data.filter(entry => entry.timestamp && new Date(entry.timestamp) > threshold);

            // Dosyaya geri yaz
            fs.writeFileSync(filePath, JSON.stringify(data, null, 2));
            console.log('Islem Basarili: ' + newEntry.kod);
          } catch (err) {
            console.error('HATA OLUSTU:', err.message);
            process.exit(1);
          }
          "

      - name: Degisiklikleri GitHub'a Kaydet
        run: |
          git config --global user.name 'Hamobus-Bot'
          git config --global user.email 'bot@royalpastanesi.com'
          git add hamobus.json
          git commit -m 'Hamobus: Veri Ana Dizine Yazildi (Emoji Temizlendi)' || echo 'Degisiklik yok'
          git push
